# Finnhub API key, used only by the server function in api/ticker.ts.
# Never prefixed with VITE_: that would ship it to the browser.
# On Vercel, set the same name under Project Settings -> Environment Variables.
API_KEY=
